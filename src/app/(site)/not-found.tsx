import { NotFoundContent } from "@/components/states/not-found-content";

/** notFound() thrown inside the site (already wrapped by the site chrome). */
export default function SiteNotFound() {
  return <NotFoundContent />;
}
